# Social automation

ShoeGlitch now treats existing SEO and release content as the source of truth for social post creation.

## What it does
- extracts content from existing structured page families
- generates Instagram-ready social draft payloads
- stores those candidates in a review-first queue
- schedules approved posts through Buffer
- syncs scheduled/published/failed states back into the queue

## Source content families
- `/releases/[slug]`
- `/how-to-clean/[slug]`
- `/worth-restoring/[slug]`
- `/release-alerts/[slug]`
- service and city SEO pages generated by `src/features/seo`

## Core files
- `src/features/social/extract.ts`
- `src/features/social/generate.ts`
- `src/features/social/store.ts`
- `src/features/social/service.ts`
- `src/features/social/buffer.ts`
- `src/features/social/auth.ts`
- `src/app/api/social/queue/route.ts`
- `src/app/api/social/queue/[id]/route.ts`
- `src/app/api/social/scan/route.ts`
- `src/app/api/social/publish/route.ts`
- `scripts/social/run-automation.ts`
- `supabase/migrations/20260422_social_automation.sql`

## Queue statuses
- `draft`
- `approved`
- `scheduled`
- `published`
- `failed`

## Review-first workflow
1. The scan job inspects existing content engines and creates queue drafts.
2. A reviewer promotes good drafts to `approved`.
3. The publish job schedules only approved drafts to Buffer.
4. A sync step updates scheduled posts to `published` or `failed`.

This keeps content generation separate from publishing and prevents blind auto-posting.

## Duplicate prevention
- the queue uses a `content_key`
- a draft key is based on:
  - route path
  - content angle
  - platform
  - source update timestamp
- the service also checks the latest queue item for the same route/angle/platform and skips unchanged content

That means a page can create a fresh social candidate when the source content actually changes, without re-drafting the same post on every scan.

## Internal-only routes
- `GET /api/social/queue`
- `POST /api/social/queue`
- `PATCH /api/social/queue/[id]`
- `POST /api/social/scan`
- `POST /api/social/publish`

These routes require:
- a `super_admin` session
- or `SOCIAL_CRON_SECRET` in `x-social-secret` or `Authorization: Bearer ...`

## Environment variables
- `SOCIAL_CRON_SECRET`
- `BUFFER_ACCESS_TOKEN` or `BUFFER_API_TOKEN`
- `BUFFER_ORGANIZATION_ID`
- `BUFFER_INSTAGRAM_CHANNEL_ID` (optional)

## Useful commands
- `npm run build`
- `npm run lint`
- `npm run typecheck`
- `npm run social:automation`

## What is live vs scaffolded
The code path is live, but queue persistence requires the Supabase migration:

```text
supabase/migrations/20260422_social_automation.sql
```

Run `npm run db:check` to confirm whether the queue table and other operational tables exist in the connected Supabase project.
- live:
  - page extraction
  - social payload generation
  - queue persistence
  - duplicate prevention
  - review/status workflow
  - Buffer publishing adapter
- requires env + migration to fully activate:
  - real Buffer scheduling
  - queue persistence in Supabase

## Best next improvement
- add a protected admin review surface for social drafts, approvals, and publish logs so queue review does not require API calls alone

That review surface now lives at `/admin/social`.
