import Experience from './Experience';

export const metadata = {
  title: 'Experience — Shoe Glitch',
  description: 'Watch the glitch become clean.',
};

export default function ExperiencePage() {
  return <Experience />;
}
